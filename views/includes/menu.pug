.list-group
        if categories.length > 0
            each category in categories
                a(href=`/categories/${category.name}`).list-group-item #{category.name}

                    
        else  
                h1 No Product                         